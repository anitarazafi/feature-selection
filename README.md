# feature-selection
Machine Learning Thesis project comparing several Feature Selection methods

## Raw Data Location
Please upload the raw dataset into: `project/data/raw/` 

And update the file path in the configuration `config/paths.yaml` as below:

`data_raw: "data/raw/ibtracs.last3years.list.v04r01.csv"`

## Development:
`python3 -m venv venv`
`source venv/bin/activate`
`pip install -r requirements.txt`
`jupyter lab`